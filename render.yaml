services:
  - type: web
    name: grantreportkit
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: out
    envVars:
      - key: NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY
        sync: false
      - key: NODE_VERSION
        value: "20"
